# VitalWatch – local deployment
# Copy to .env only if you need to override defaults.
#
# By default the app uses SQLite (vitalwatch.db in project root).
# No .env file is required for local run.
#
# --- Optional: use PostgreSQL locally ---
# USE_POSTGRES=1
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=vitalwatch
# POSTGRES_USER=admin
# POSTGRES_PASSWORD=password
#
# --- Or set a full URL (overrides above) ---
# DATABASE_URL=sqlite:///./vitalwatch.db
# DATABASE_URL=postgresql://user:pass@localhost:5432/vitalwatch
